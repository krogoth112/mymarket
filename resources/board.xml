<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
"http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="board">
	<typeAlias alias="BoardVo" type="com.bit.mymarket.vo.BoardVo" />
	<resultMap class="BoardVo" id="resultMapList">
		<result column="no" property="no" />
		<result column="title" property="title" />
		<result column="content" property="content" />
		<result column="reg_date" property="regDate" />
		<result column="VIEW_COUNT" property="viewCnt" />
		<result column="user_no" property="userNo" />
		<result column="user_name" property="userName" />
	</resultMap>

	<!-- <select id="search" resultMap="resultMapList"> <![CDATA[ SELECT * from 
		BOARD where content=#content#, title=#title# ]]> </select> <insert id="insert" 
		parameterClass="BoardVo"> <![CDATA[ insert into BOARD (NO, TITLE, CONTENT, 
		MEMBER_NO, MEMBER_NAME, REG_DATE) VALUES (board_seq.nextval, #title# , #content#, 
		#memberNo#, #memberName#, sysdate) ]]> </insert> <delete id="delete" parameterClass="java.lang.Long"> 
		<![CDATA[ delete from BOARD where no=#no# ]]> </delete>  <update id="update" parameterClass="BoardVo"> 
		<![CDATA[ UPDATE board SET title=#title#, content=#content# WHERE no=#no# 
		]]> </update> <select id="getbyno" parameterClass="java.lang.Long" resultMap="resultMapList"> 
		<![CDATA[ SELECT no, title, content, member_no, member_name, view_cnt, TO_CHAR(reg_date, 
		'HH12:MI') as reg_date FROM board where no=#no# ]]> </select> 
		
		 <select id="listkwd" resultMap="resultMapList" parameterClass="java.lang.String"> 
		<![CDATA[ SELECT no, title, content, member_no, member_name, view_cnt, TO_CHAR(reg_date, 
		'YY-MM-DD HH12:MI') as reg_date from board where title LIKE '%' ||#kwd# || 
		'%' or content LIKE '%' ||#kwd# || '%' order by no desc ]]> </select> -->
	<select id="list" resultMap="resultMapList">
		<![CDATA[
		SELECT NO, TITLE,content, TO_CHAR(reg_date, 'HH12:MI') as reg_date, VIEW_COUNT, User_no, USER_NAME 
		FROM BOARD_COMMNITY
		order by no desc
		]]>
	</select>
	
	<select id="count" resultClass="java.lang.Integer" > <![CDATA[ SELECT count(*) from BOARD_COMMNITY ]]> 
		</select>
		
		<select id="view" 
		parameterClass="java.lang.Long" resultMap="resultMapList"> <![CDATA[ select 
		* from BOARD_COMMNITY where no=#no# ]]> </select>

</sqlMap>

