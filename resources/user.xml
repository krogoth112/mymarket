<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
"http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="user">
	<typeAlias alias="UserVo" type="com.bit.mymarket.vo.UserVo" />
	<select id="login" parameterClass="UserVo" resultClass="UserVo">
		<![CDATA[
			select NO as no, PHONE_NUMBER as phoneNumber, PASSWORD as password, USER_NAME as name, GENDER as gender, EMAIL as email, REG_DATE as regDate, MEMBER_TYPE as memberType
			from MEMBER
			where EMAIL=#email# 
			and PASSWORD=#password#
		]]>
	</select>
</sqlMap>